# Sao chép file này thành .env rồi điền giá trị THẬT (đừng commit .env lên git)
#   cp .env.example .env

# Cổng web mở ra ngoài (mặc định 8080). Thường đặt sau reverse proxy / domain.
WEB_PORT=8080

# PostgreSQL
POSTGRES_USER=miki
POSTGRES_DB=miki
POSTGRES_PASSWORD=doi_mat_khau_db_manh_o_day

# Chuỗi bí mật ký JWT — đặt chuỗi ngẫu nhiên dài (vd: openssl rand -hex 32)
JWT_SECRET=doi_thanh_chuoi_ngau_nhien_that_dai

# Tài khoản admin khởi tạo lần đầu (đăng nhập trang admin.html)
ADMIN_EMAIL=admin@mikimakeup.vn
ADMIN_PASSWORD=doi_mat_khau_admin_o_day
